namespace StudentSystem.Tests
{
    using Models;
    using Models.Utils;
    using NUnit.Framework;
    using System;

    [TestFixture]
    public class TestDatGenerator
    {
        //[Test]
        //public void CreatingMultipleStudents_ShouldThrowOutOfRangeException()
        //{
        //    var start = DataGenerator.GenerateId();

        //    for (int i = start; i < Constants.ID_MAX_VALUE; i++)
        //    {
        //        if (i > Constants.ID_MAX_VALUE)
        //        {
        //            Assert.Throws<ArgumentOutOfRangeException>(() => new Student(i.ToString()));
        //        }

        //        var student = new Student(i.ToString());
        //    }
        //}
    }
}
